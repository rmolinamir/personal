# Bitcoin Form

Build a Bitcoin Form component in React + TypeScript.

Requirements:

* USD amount input
* estimated BTC output
* available balance
* fee row
* buy button with disabled state
* bitcoin quote integration
* format money and BTC values with `Intl`
* preserve last valid parsed value if the user types junk
