{
  "name": "@acme/tsconfig",
  "description": "Shared TypeScript configurations for different environments (Node.js, DOM, React, Next.js)",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "files": [
    "*.json"
  ],
  "scripts": {
    "check": "biome check",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  }
}
